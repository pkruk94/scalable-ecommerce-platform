spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  config:
    import: "optional:configserver:"
  application:
    name: product-service

eureka:
  client:
    enabled: false